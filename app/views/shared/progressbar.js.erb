let step = 'step1';

const step1 = document.getElementById('step1');
const step2 = document.getElementById('step2');
const step3 = document.getElementById('step3');

const next_button = document.querySelector('#next');
const back_button = document.querySelector('#back');

next_button.addEventListener('click', (event) => {
  event.preventDefault();
  console.log("HALLO") // <-- to prevent <form>'s native behaviour
});

back_button.addEventListener('click', (event) => {
  event.preventDefault();
  console.log("TSCHÜSSs") // <-- to prevent <form>'s native behaviour
});



document.querySelector(".btn-shadow").addEventListener("click", function() {
  console.log("Event")
})

function next() {
  if (step === 'step1') {
    step = 'step2';
    step1.classList.remove("is-active");
    step1.classList.add("is-complete");
    step2.classList.add("is-active");

  } else if (step === 'step2') {
    step = 'step3';
    step2.classList.remove("is-active");
    step2.classList.add("is-complete");
    step3.classList.add("is-active");

  } else if (step === 'step3') {
    step = 'step4d';
    step3.classList.remove("is-active");
    step3.classList.add("is-complete");
    step4.classList.add("is-active");

  } else if (step === 'complete') {
    step = 'step1';
    step3.classList.remove("is-complete");
    step2.classList.remove("is-complete");
    step1.classList.remove("is-complete");
    step1.classList.add("is-active");
  }
}
